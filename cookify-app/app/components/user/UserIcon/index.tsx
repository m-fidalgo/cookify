import { Icon } from 'app/components/common';

import { RoundView } from './styles';

export const UserIcon: React.FC = () => {
  return (
    <RoundView>
      <Icon color="aqua" name="person" />
    </RoundView>
  );
};
