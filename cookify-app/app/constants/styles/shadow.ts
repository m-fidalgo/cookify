import { HUES } from './palette';

export const DEFAULT_SHADOW_STYLES = {
  shadowColor: HUES.lightGray,
  shadowOffset: { width: 2, height: 2 },
  shadowOpacity: 1,
  shadowRadius: 2,
  elevation: 4,
};
